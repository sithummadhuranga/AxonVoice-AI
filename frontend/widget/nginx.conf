server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index widget-frame.html;

    location / {
        try_files $uri =404;
    }

    location ~* \.(html|js|css|map)$ {
        add_header Cache-Control "public, max-age=300";
    }
}
